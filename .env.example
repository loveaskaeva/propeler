# Для локальной разработки (SQLite) — оставьте как есть
DATABASE_URL=sqlite:///./propeler.db

# Для PostgreSQL — пример:
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/propeler

# Порт приложения
PORT=8000
